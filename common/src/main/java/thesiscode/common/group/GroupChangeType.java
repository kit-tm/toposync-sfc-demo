package thesiscode.common.group;

/**
 * Indicates what change happened at a group change.
 */
public enum GroupChangeType {
    /**
     * a host joined the group
     */
    JOIN, /**
     * a host left the group
     */
    LEAVE
}
